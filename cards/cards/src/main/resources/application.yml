server:
  port: 9000
spring:

#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest

  profiles:
    active: "prod"

  datasource:
    url: jdbc:mysql://localhost:3308/cardsdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  config:
    import: "optional:configserver:http://localhost:8071/"
  application:
    name: "cards"

management:
  endpoints:
    web:
      exposure:
        include: "*"